{
  "name": "@atomic/orchestrator",
  "version": "1.0.0",
  "description": "Mission orchestrator with checkpoints and reversible batches",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@atomic/types": "1.0.0",
    "@atomic/schemas": "1.0.0",
    "@atomic/utils": "1.0.0",
    "express": "^4.18.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "ts-node-dev": "^2.0.0"
  }
}
